
# https://oeis.org/A002392
# old-school, midi ... oeis.org/play
# https://stackoverflow.com/a/14168174/184614
# DON'T scape us ... FAIR-USE baby 
# I like them, so I will not harvest them, just use search and one-offs
# https://oeis.org/stripped.gz
# https://oeis.org/names.gz
# The publication info they want to protect, not the sequences, GOOD!

oeis.local = function()
	{
	# "stripped", "names" 
	# download *.gz 
	# stripped is 
	# names is A002392|Decimal expansion of natural logarithm of 10.
	# stripped is A002392|,2,3,0,2,5,8,5,0,9,2,9,9,4,0, ....
	# 2.302585 ... 	OFFSET	1,1
	# map four parallel lists ... 
	# name|title|decimal (as.string)|sequence (as integer vector)
	# A002162|Decimal expansion of the natural logarithm of 2.|0.693147180559|,6,9,3,1,4,7,1,8,0,5,5,9,9,4,5,3,0,9,4,1,7,2,3,2,1,2,1,4,5,8,1,7,6,5,6,8,0,7,5,5,0,
	# website has offset ... OFFSET	0,1
	# how do I know?  ... I have to store what I have ... 
	# offset = 1,1 means MULTIPLY * 10 
	# store sequence as-is ... can search on it ... wildcard 
	}


.oes = function(key="A002162", offset=c(0,1), to.numeric=FALSE)
	{
	
	
	}

oeis.init = function()
	{
	
	}
	
	
oies.lookup = function(key=3.14159)
	{
	# get/cache search page 
	# get/cache lookup page 
	
	
	}


